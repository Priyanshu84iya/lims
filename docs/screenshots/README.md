# Screenshots

Add real UI screenshots of the application here, then reference them from the root `README.md`.

Suggested captures (PNG, ~1600px wide, dark or light theme):

| Filename | What to capture |
|---|---|
| `home.png` | Public landing page (`/`) |
| `login.png` | Admin / Lab login mode selector (`/login`) |
| `dashboard.png` | Lab dashboard with stat cards (`/dashboard`) |
| `reception.png` | Patient registration form (`/reception`) |
| `report-new.png` | Report creation workspace (`/reports/new`) |
| `report-pdf.png` | Generated PDF report preview |
| `download-report.png` | Public report download page (`/download-report`) |
| `chatbot.png` | LIMS Assistant chat widget (public pages) |
| `admin-labs.png` | Admin lab management (`/admin`) |
| `pwa-install.png` | PWA install prompt / standalone app |

After adding files, replace the placeholder table in the root `README.md` (search for
"screenshot placeholders") with image tags pointing to `docs/screenshots/<name>.png`.
