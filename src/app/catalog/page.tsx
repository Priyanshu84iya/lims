"use client";

import Link from "next/link";
import { useEffect, useMemo, useState } from "react";
import { FlaskConical, Search } from "lucide-react";
import { LABORATORY_TESTS } from "@/lib/laboratory/registry";

type CustomTest = { code: string; definition: string };

export default function CatalogPage() {
  const [search, setSearch] = useState("");
  const [customTests, setCustomTests] = useState<CustomTest[]>([]);
  useEffect(() => { fetch("/api/custom-tests").then((response) => response.json()).then((data) => setCustomTests(data.tests || [])); }, []);
  const groups = useMemo(() => Array.from(new Set(LABORATORY_TESTS.map((test) => test.category))).map((category) => ({ category, tests: LABORATORY_TESTS.filter((test) => test.category === category && `${test.name} ${test.code}`.toLowerCase().includes(search.toLowerCase())) })).filter((group) => group.tests.length), [search]);
  return <main className="min-h-screen bg-[#f5f8f7] text-slate-900"><header className="border-b border-slate-200 bg-white"><div className="mx-auto flex max-w-[1440px] items-center justify-between px-6 py-4 lg:px-10"><Link href="/dashboard" className="flex items-center gap-3"><span className="flex size-10 items-center justify-center rounded-xl bg-teal-700 text-white"><FlaskConical size={21} /></span><span><span className="block text-sm font-bold tracking-[0.18em] text-teal-800">NORTHSTAR</span><span className="block text-xs text-slate-500">Laboratory information system</span></span></Link><nav className="flex gap-2 text-sm font-medium"><Link href="/reports/new" className="rounded-lg px-3 py-2 text-slate-600 hover:bg-slate-100">New report</Link><Link href="/reports" className="rounded-lg px-3 py-2 text-slate-600 hover:bg-slate-100">Reports</Link><Link href="/catalog" className="rounded-lg bg-teal-50 px-3 py-2 text-teal-800">Catalog</Link></nav></div></header><div className="mx-auto max-w-[1440px] px-6 py-8 lg:px-10"><p className="text-xs font-bold uppercase tracking-[0.2em] text-teal-700">Laboratory configuration</p><div className="mt-2 flex flex-col justify-between gap-4 md:flex-row md:items-end"><div><h1 className="text-3xl font-semibold tracking-tight">Pathology test catalog</h1><p className="mt-2 text-sm text-slate-500">A–Z investigation registry with category and parameter coverage.</p></div><label className="relative"><Search size={16} className="absolute left-3 top-3 text-slate-400" /><input className="w-full rounded-lg border border-slate-200 bg-white py-2.5 pl-9 pr-3 text-sm outline-none focus:border-teal-500 md:w-72" value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Search catalog" /></label></div><div className="mt-8 grid gap-6 md:grid-cols-2 lg:grid-cols-3">{groups.map((group) => <section key={group.category} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-center justify-between border-b border-slate-100 pb-3"><h2 className="font-semibold">{group.category}</h2><span className="text-xs text-slate-400">{group.tests.length} tests</span></div><div className="mt-3 space-y-1">{group.tests.map((test) => <div key={test.code} className="flex items-center justify-between rounded-lg px-2 py-2 hover:bg-slate-50"><div><p className="text-sm font-medium">{test.name}</p><p className="text-xs text-slate-400">{test.code} · {test.parameters.length} parameter{test.parameters.length !== 1 ? "s" : ""}</p></div></div>)}</div></section>)}</div>{customTests.length > 0 && <section className="mt-8 rounded-2xl border border-teal-200 bg-teal-50 p-6"><h2 className="font-semibold text-teal-900">Saved custom tests</h2><div className="mt-3 grid gap-3 md:grid-cols-3">{customTests.map((test) => <div key={test.code} className="rounded-lg border border-teal-100 bg-white p-3"><p className="font-medium">{test.code}</p><p className="mt-1 text-xs text-slate-500">Custom definition available</p></div>)}</div></section>}</div></main>;
}
